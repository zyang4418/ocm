services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ocm-backend-dev
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - /app/tmp
    environment:
      - PORT=8080
    command: air
