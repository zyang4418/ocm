# Mosquitto config for the CI integration job only: no auth, no persistence.
# Production credentials and ACLs live in deploy/mosquitto (dynsec), never here.
listener 1883
allow_anonymous true
persistence false
