{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/zyang4418/ocm/blob/main/iot/spec/schema/event.json",
  "title": "Device event (iot/{site}/{sourceId}/{deviceId}/event)",
  "description": "Asynchronous device-reported event (alarm, anomaly). Not retained; deliveries are QoS1 at-least-once and deduplicated by the registry on (deviceId, type, at, payload).",
  "type": "object",
  "required": ["v", "at", "type"],
  "additionalProperties": false,
  "properties": {
    "v": { "const": 1 },
    "at": {
      "type": "integer",
      "minimum": 1,
      "description": "Event time, Unix milliseconds UTC."
    },
    "type": {
      "type": "string",
      "minLength": 1,
      "maxLength": 64,
      "pattern": "^[\\x21-\\x7E]+$",
      "description": "Event type, printable ASCII without spaces (e.g. door.forced)."
    },
    "data": {
      "type": "object",
      "description": "Optional structured detail."
    }
  }
}
